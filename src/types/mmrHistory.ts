export type mmrHistoryType = {
  currenttier: string;
  currenttierpatched: string;
  images: {
    icon: string;
    portrait: string;
  };
  mmr_change_to_last_game: number;
  elo: number;
  date: string;
  date_raw: string;
};
